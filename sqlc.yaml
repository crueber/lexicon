version: "2"
sql:
  - engine: "sqlite"
    queries: "internal/user/queries.sql"
    schema: "migrations/"
    gen:
      go:
        package: "user"
        out: "internal/user"
        emit_json_tags: true
        emit_empty_slices: true
  - engine: "sqlite"
    queries: "internal/library/queries.sql"
    schema: "migrations/"
    gen:
      go:
        package: "library"
        out: "internal/library"
        emit_json_tags: true
        emit_empty_slices: true
  - engine: "sqlite"
    queries: "internal/book/queries.sql"
    schema: "migrations/"
    gen:
      go:
        package: "book"
        out: "internal/book"
        emit_json_tags: true
        emit_empty_slices: true
  - engine: "sqlite"
    queries: "internal/task/queries.sql"
    schema: "migrations/"
    gen:
      go:
        package: "task"
        out: "internal/task"
        emit_json_tags: true
        emit_empty_slices: true
  - engine: "sqlite"
    queries: "internal/reader/queries.sql"
    schema: "migrations/"
    gen:
      go:
        package: "reader"
        out: "internal/reader"
        emit_json_tags: true
        emit_empty_slices: true
