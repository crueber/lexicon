version: "2"
sql:
  - engine: "sqlite"
    queries: "internal/user/queries.sql"
    schema: "migrations/"
    gen:
      go:
        package: "user"
        out: "internal/user"
        emit_json_tags: true
        emit_empty_slices: true
  - engine: "sqlite"
    queries: "internal/library/queries.sql"
    schema: "migrations/"
    gen:
      go:
        package: "library"
        out: "internal/library"
        emit_json_tags: true
        emit_empty_slices: true
  - engine: "sqlite"
    queries: "internal/book/queries.sql"
    schema: "migrations/"
    gen:
      go:
        package: "book"
        out: "internal/book"
        emit_json_tags: true
        emit_empty_slices: true
  - engine: "sqlite"
    queries: "internal/task/queries.sql"
    schema: "migrations/"
    gen:
      go:
        package: "task"
        out: "internal/task"
        emit_json_tags: true
        emit_empty_slices: true
  - engine: "sqlite"
    queries: "internal/reader/queries.sql"
    schema: "migrations/"
    gen:
      go:
        package: "reader"
        out: "internal/reader"
        emit_json_tags: true
        emit_empty_slices: true
  - engine: "sqlite"
    queries:
      - "internal/shelf/queries.sql"
      - "internal/shelf/magic_queries.sql"
    schema: "migrations/"
    gen:
      go:
        package: "shelf"
        out: "internal/shelf"
        emit_json_tags: true
        emit_empty_slices: true
  - engine: "sqlite"
    queries: "internal/metadata/queries.sql"
    schema: "migrations/"
    gen:
      go:
        package: "metadata"
        out: "internal/metadata"
        emit_json_tags: true
        emit_empty_slices: true
  - engine: "sqlite"
    queries: "internal/notebook/queries.sql"
    schema: "migrations/"
    gen:
      go:
        package: "notebook"
        out: "internal/notebook"
        emit_json_tags: true
        emit_empty_slices: true
